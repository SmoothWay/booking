http:
  port: 8080

db:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: booking
  migration_dir: db/migrations

redis:
  addr: localhost:6379
  password: redis
  db: 0

kafka:
  brokers:
    - localhost
  group_id: booking